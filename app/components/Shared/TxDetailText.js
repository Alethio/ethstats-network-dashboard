import React from 'react';
import styled from 'styled-components';

const TxDetailText = styled.div`
  color: #FFFFFF;
  font-size: 12px;
  font-weight: bold;
  & .pull-right {
    float: right;
  }
`;

export default TxDetailText;
